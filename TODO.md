# LuxAPI Development Roadmap

## 🔴 CRITICAL — Fix Before Anything Else
*Found 2026-08-13 via deep code-level audit (content-level, not just structure) of `commons`/`core`/`fabric`/`neoforge`/`bukkit`. Verified by direct read, not just agent report — full call chains traced.*
- [ ] **Permission enforcement is completely bypassed on Fabric and NeoForge for every annotation-driven command.** `commons/command/CommandProcessor.kt:48,56` correctly gates `@Command`/`@SubCommand` execution on `sender.hasPermission(permission)` — the check itself is real and correct. But `fabric/player/FabricLuxPlayer.kt:42-44` and `neoforge/player/NeoForgeLuxPlayer.kt:44-47` both hardcode `hasPermission(permission: String): Boolean { return true }` (NeoForge's has `// TODO: Map to NeoForge PermissionAPI later if needed`; Fabric's has no comment at all). `FabricCommandManager.kt:102-108` / `NeoForgeCommandManager.kt:102-108` both wrap the executing player as exactly this class before calling `processor.process()`. **Net effect: any player, with any or no permissions, can execute any permission-gated command registered through the Fabric/NeoForge command framework — on both of LuxAPI's two actual Cobblemon-supporting platforms.** Tests bake the bug in as expected behavior: `FabricLuxPlayerTest.kt:60` / `NeoForgeLuxPlayerTest.kt:55` both assert `hasPermission(...) == true` unconditionally — no test exists for the denied path because the implementation can't produce it. `bukkit/command/BukkitCommandSender.kt:46-48` does this correctly (delegates to native `sender.hasPermission(permission)`) — Bukkit is not affected. Fix: wire real permission checks (Fabric permissions API / NeoForge equivalent, or at minimum vanilla op-level as an interim floor) into both `hasPermission()` implementations, and add a test that exercises an actual denial.
- [ ] **Console/command-block execution is silently dead on Fabric and NeoForge.** Same two files, `FabricCommandManager.kt:101-109` / `NeoForgeCommandManager.kt:101-109`: `executeCommand()` sets `sender = null` when `!source.isPlayer`, then only calls `processor.process(...)` `if (sender != null)` — for console or command-block execution this is a complete silent no-op: no error message, no log, nothing happens. `CommandProcessor.buildArgumentsForMethod()` (commons) explicitly supports a plain `CommandSender` first parameter (not just `LuxPlayer`), meaning non-player execution was intended to work — the platform managers just never wire it in. `bukkit/command/BukkitCommandWrapper.kt:30-36` forwards any `CommandSender` (player or console) unconditionally — Bukkit is not affected. Fix: build a console-sender wrapper for both platforms (mirroring what `BukkitCommandSender` already does) instead of discarding non-player invocations.
- [ ] **`commons/event/EventBus.kt:11` — not thread-safe, inconsistent with the rest of the codebase's own pattern.** `listeners` is a plain `mutableMapOf`, while every other central registry in the same package uses `ConcurrentHashMap` (`ServiceManager.kt:12`, `MetadataContainer.kt:11`, `PlayerMetadataManager.kt:14`). `register()`/`unregister()`/`fire()` all mutate/iterate this map with zero synchronization. If any code fires or (un)registers events off the main thread (async DB callback, scheduler thread — both exist in this codebase), this is a real concurrent-modification risk, not hypothetical. Fix: switch to `ConcurrentHashMap` to match the established pattern.
- [ ] **Permission system is inconsistently implemented across every path that isn't Bukkit.** Beyond the Fabric/NeoForge stub above: `core/command/MinecraftCommandSender.kt:44-46` (the separate Brigadier-native path used by `core/command/BrigadierCommandAdapter.kt`) ignores the `permission` string argument entirely and always checks vanilla op-level 2 (`source.hasPermission(2)`) — not broken the same way, but the API shape promises per-node granularity it doesn't deliver. Only Bukkit's implementation actually does what the `CommandSender.hasPermission(String)` contract promises. Worth resolving as one pass alongside the critical fix above, not two separate efforts.

*Minor, non-urgent findings from the same audit, folded into Phase 13 below rather than listed here: `core/loot/LootManager.kt:24-27` silently no-ops on an unregistered `lootId` (no log); `commons/config/LuxConfig.kt:40-52`'s `reload()` would clobber any future non-persisted field on a `LuxConfig` subclass (currently inert, no such field exists yet, but a latent footgun).*

## 🟢 Phase 1: Multi-loader Architecture & Core Infrastructure (Completed)
*The baseline foundation that allows LuxAPI to run seamlessly on both Fabric and NeoForge.*
- [x] **Platform-Agnostic Bridge:** Unified `commons` and `core` modules mapping directly to `fabric` and `neoforge` implementations.
- [x] **Command Engine API:** Advanced Brigadier wrapper (`CommandEngine`, `InjectorRegistry`, `TabRegistry`) for creating complex server commands effortlessly.
- [x] **Universal Scheduler:** Cross-platform tick-based task scheduling (`LuxScheduler`, `TaskData`).
- [x] **Interactive GUI Builder:** Fluent API for chest GUIs and paginated menus (`GuiBuilder`, `PaginatedGuiBuilder`, `LuxMenu`).
- [x] **Player Abstraction:** `LuxPlayer` and `PlayerManager` for cross-platform player handling.

## 🟢 Phase 2: Data, Utility & External Hooks (Completed)
*Essential utilities for server-side developers.*
- [x] **Database & Metadata API:** Built-in `HikariDatabaseProvider` and `DatabaseService` for async database queries.
- [x] **Persistent Attributes:** `AttributeManager` and `MetadataContainer` for safely attaching custom data to players and entities.
- [x] **Discord Integration:** Out-of-the-box `DiscordWebHook` and `DiscordEmbed` builders for seamless server-to-Discord logging and events.
- [x] **Text & Chat Utilities:** `SmartMessage`, `PlaceholderManager`, and `ChatPaginator` for deep chat formatting and i18n (`LanguageManager`).
- [x] **Item & Inventory API:** `ItemBuilder` and `InventoryUtils` for safe, cross-platform item manipulation.
- [x] **Math & Cooldowns:** Built-in `CooldownManager`, `TimeGateManager`, `Cuboid` region math, and `RandomWeightedSet` tools.

## 🟢 Phase 3: Cobblemon World Boss Framework (Completed)
*The first fully realized Cobblemon-specific module.*
- [x] **WildPokemonBuilder API:** Fluent builder pattern for instantiating customized wild Pokémon (custom IVs/EVs, scale, tags, max health overrides).
- [x] **Boss UI & Scoreboard Integration:** Real-time Boss Bar synchronization (`BossHpSynchronizer`) and dynamic DPS scoreboard (`ScoreboardManager`) rendering without client-side mods.
- [x] **Smart Aggro & Targeting System:** `BossAggroManager` and `BossDamageListener` to track player DPS and manage entity threat tables.
- [x] **Minion & Phase Management:** `BossPhaseManager` for health-threshold triggers and `BossMinionManager` for dynamic add-spawns (Totem calls).
- [x] **Boss Event Hooks:** `LuxBossHooks` registry (`onBossSpawned`, `onPhaseChanged`, `onBossDefeated`) for external plugin integrations (e.g., loot distribution, quest progression).

## 🟢 Phase 4: Story-Driven Dialogue & NPC Wrapper (Completed)
- [x] **DialogueSequenceAPI:** `DialogueBuilder` — fluent multi-page dialogue trees with text/choice/input/auto-advance pages, custom speakers (player-skin, NPC-model, and Pokémon-species/`addPokemonSpeaker` portraits), dynamic per-render text (`addDynamicPage`), custom timeout/escape/init actions (`onTimeout`, `onEscape`, `onInitialize`, page-level `escapeAction`), a code↔JSON-registry bridge (`buildAndRegister`/`openRegistered`), and background/gibber customization. Implemented — closed all 6 gaps found against Cobblemon `1.7.3`'s `api/dialogue` surface. **Known fragility:** touching these files surfaced 4 latent (pre-existing, not introduced this session) compiler warnings — `Cannot access class 'com.bedrockk.molang.Expression'... may be forbidden soon` — same root cause as the AI-task-wrapper classpath gap (`com.bedrockk.molang` not on the compile classpath, `isTransitive = false`). Currently harmless (build succeeds), but worth a real fix (see Phase 13) before a future Kotlin compiler version turns it into a hard error.
- [x] **NPC Action Wrapper:** `NPCExtensions.initiateBattle()` covers battle initiation; `NPCFxExtensions.kt` adds `NPCEntity.playAnimation(BattleAnimation)` (enum-safe wrap of Cobblemon's own `SEND_OUT`/`RECALL`/`WIN`/`LOSE` battle animations) and `NPCEntity.playBattleTheme()`. Note: no separate `playAnimation(String, ...)`/`playSound(...)` wrappers were added — `NPCEntity` already exposes both as public members (`PosableEntity.playAnimation`, vanilla `Entity.playSound`) with identical signatures, so a same-signature extension would be dead, compiler-shadowed code; call those directly on the entity instead. Implemented.
- [x] **Condition-based Dialogue Routing:** `ChoicePageBuilder.option(isVisible, isSelectable, ...)` accepts arbitrary predicate lambdas over `ActiveDialogue`/player state — developers can already route on inventory, active Pokémon, or custom tags this way. Implemented.

## 🟢 Phase 5: Battle Scripting & Interceptor API (Completed, rescoped to Cobblemon's real API)
- [x] **CustomBattleBuilder:** `LuxBattleBuilder` (`battle/BattleBuilder.kt`) + `BattleExtensions.kt` — wild/player/NPC forced battles, double-battle toggle. Implemented.
- [x] **BattleRuleManager:** `BattleRuleManager` — species bans, level caps, and a custom-rule escape hatch, enforced by cancelling Cobblemon's `BattleStartedEvent.Pre` before the battle launches. **Scope correction vs. original wording:** item restrictions and weather constraints are dropped entirely — verified against Cobblemon `1.7.3` source that no hook exists for either (its own `BattleFormat.ruleSet` only accepts a fixed set of Showdown clause strings; Cobblemon's own maintainer comment calls the closest thing, `adjustLevel`, a "stop gap... before a more general system is implemented"). `BattleStartedEvent.Pre` is the only real cancellable pre-battle hook, so species/level are the only enforceable constraints today. Implemented.
- [x] **Battle Event Interceptors:** `BattleInterceptor` — `onHealthThreshold(battle, target, threshold, action)` for cutscene-style triggers and `stopBattle(battle)` (wraps `PokemonBattle.stop()`, the correct general-purpose method — distinct from `BattleManager.executeCrashSafeTurn`'s lower-level `closeBattle()`, which is crash-recovery-only and skips normal teardown). **Implementation note:** Cobblemon has no HP-changed event for in-battle Pokémon (confirmed via source grep — zero results), so thresholds are evaluated via 1-second `LuxScheduler` polling per active battle, not event subscription; poll tasks are cleaned up on `BATTLE_VICTORY` via `npc/battle/BattleEventListener.kt`. **Known gap:** cleanup only hooks `BATTLE_VICTORY` — a battle ended by fleeing or by `stopBattle()` itself leaks its poll task until server restart; flagged during design, not fixed in this pass. Implemented.

## 🟢 Phase 6: Safe Storage & Party Transactions (Completed)
- [x] **PokemonTransactionAPI:** `PCStorageManager.depositToPC()`/`withdrawFromPC()` (+ `LuxPlayer.depositPokemonToPC()`/`withdrawPokemonFromPC()` extensions) — mirrors Cobblemon's own `MovePartyPokemonToPCHandler`/`MovePCPokemonToPartyHandler` packet-handler logic exactly (`store.remove()` then `otherStore[position] = pokemon`, fully packet-synced via Cobblemon's own `PokemonStore` overrides), including the `preventCompletePartyDeposit` server-config guard. **Deliberate deviation from upstream:** if an explicit destination slot is given and already occupied, this fails loud (`false`) instead of silently redirecting to the first available slot the way Cobblemon's in-game PC screen does — a raw dev-facing API silently placing a Pokémon somewhere other than the coordinates the caller specified is a footgun. Implemented.
- [x] **Anti-Duplication Guards (PC-side):** `PCStorageManager.move()` now delegates to `PCStore.swap()` instead of raw box mutation, closing the client-desync dupe-risk found this session. Note: a separate, more serious duplication/data-loss gap exists in the *GTS* system — see Phase 8.
- [x] **Property Manipulation Wrapper:** `PokemonPropertyManager` (+ `Pokemon.setNature()`/`setHeldItem()` extensions) — safe, sync-aware IV (0-31), EV (0-252/510 total), nature, and held-item mutation. IV/EV delegate to Cobblemon's own `Pokemon.setIV()`/`setEV()` members (not the raw `ivs[stat]=`/`evs[stat]=` operators — those skip the max-HP recalculation Cobblemon's own methods do when the HP stat itself changes, a correctness bug caught during implementation), nature/held-item delegate to the `Pokemon.nature` setter and `Pokemon.swapHeldItem()`; Cobblemon itself handles client sync and persistence-dirty marking regardless of which store the Pokémon currently lives in. **Note:** no `Pokemon.setIV()`/`setEV()` extension wrappers were added — `Pokemon` already exposes public members with the identical signature, so a same-signature extension would be dead, compiler-shadowed code (same pattern as the `NPCFxExtensions` case in Phase 4); call `PokemonPropertyManager.setIV()`/`setEV()` directly for the validated Boolean-return version, or `pokemon.setIV()`/`setEV()` directly for raw Cobblemon behavior. **Deliberate deviation from upstream:** `setHeldItem()` defaults `decrement = false`, opposite of `swapHeldItem()`'s own default of `true` — that default exists for consuming an item out of a player's hand mid-interaction, which doesn't apply to a dev constructing an `ItemStack` value to assign as a property. Implemented.

## 🟢 Phase 7: Dynamic Spawning & Area Controllers (Completed)
- [x] **AreaSpawnManager:** `AreaSpawnManager` (`spawning/AreaSpawnManager.kt`) — register/unregister region-scoped `SpawnArea`s (dimension + `Cuboid` region + spawn-weight multiplier + banned species), enforced via a single stateless `LuxAreaSpawningInfluence` wired into Cobblemon's real `PlayerSpawnerFactory.influenceBuilders`. **Scope note:** uses LuxAPI's own axis-aligned `Cuboid` for regions rather than Cobblemon's native spherical `SpatialSpawningZoneInfluence` — that system is tied to ephemeral per-tick zone-generation internals with no stable global attachment point, whereas `PlayerSpawnerFactory` is a genuinely public, one-time registration point and lets the API reuse a type devs already know from Phase 2. The influence live-queries the registry on every spawn check (never a snapshot), so area changes apply immediately even to players who already have a spawner. Implemented.
- [x] **DynamicEncounterAPI:** `SwarmManager`/`SwarmEvent` — timed swarm/outbreak events with species, level range, radius, and active-entity cap, driven by a self-cancelling scheduled task. Implemented.
- [x] **Spawn Event Interception:** `SpawnInterceptor.onPokemonSpawn()` — wraps Cobblemon's own `CobblemonEvents.POKEMON_ENTITY_SPAWN` (fired for every natural spawn attempt, before the entity enters the world) in a plain listener callback carrying entity/world/position/species and a `cancel()`, so devs don't need to touch Cobblemon's `Observable`/`Cancelable` plumbing directly. `PokemonSpawner.kt`'s direct-spawn helpers are unchanged — this is a separate, complementary hook into *natural* spawns, not a replacement for manual spawning. Implemented.

## 🟢 Phase 8: Economy & Trade Hooks (Completed)
- [x] **TradeInterceptorAPI:** `TradeInterceptor` (`trade/TradeInterceptor.kt`) — `onTradeAttempt()`/`onTradeCompleted()` wrap Cobblemon's real `CobblemonEvents.TRADE_EVENT_PRE`/`TRADE_EVENT_POST`, fired by its native player-to-player `TradeManager`. Same cancelable-listener pattern as Phase 7's `SpawnInterceptor`. Implemented.
- [x] **GTS/Economy Helpers (appraisal):** `PokemonPriceCalculator` + `LuxPokemonEconomy`'s pluggable `PokemonAppraiser` chain for dynamic valuation. Implemented (carried over from the prior pass).
- [x] **GTS Listing/Purchase — dupe/data-loss bug fixed:** `GlobalTradeManager.listPokemon()` now really serializes the Pokémon via the existing `PokemonSerializer` and removes it from the seller's party (respecting the `preventCompletePartyDeposit` guard, same rule as Phase 6's PC deposit). `purchaseListing()` deserializes the *real* listed Pokémon and verifies the buyer has party/PC space *before* any funds move — a buyer is never charged for a trade that can't complete — then grants the actual Pokémon (not a blank placeholder). **Added beyond the original TODO wording:** `cancelListing()`, so a listed Pokémon isn't permanently stuck unless it sells — the listing is only removed once the Pokémon has actually been handed back to the seller. Implemented.
- [x] **Trade Tax Injection:** `TradeTaxManager` (`economy/TradeTaxManager.kt`) — pluggable `TradeTaxCalculator` chain, same fold-over-registered-rules shape as `LuxPokemonEconomy`'s appraisers, applied to the seller's payout at GTS purchase time. Result is clamped to `[0, price]` (deliberate deviation from the unclamped appraiser chain — this number directly moves real funds, so a misconfigured rule can't invert or drain a payout). No calculators registered → 0% tax, fully backward compatible. **Scope note:** tax only applies to GTS purchases — vanilla Cobblemon player-to-player trades carry no currency, so there's no attachment point there without inventing a currency side-channel nothing else in the codebase has. Implemented.

## 🟢 Phase 9: Cinematic & MoLang FX Wrapper (Completed)
- [x] **MoLangCinematicAPI:** `MoLangCinematicAPI` (`fx/MoLangCinematicAPI.kt`) — generic wrapper around Cobblemon's real `ActionEffectTimeline`/`ActionEffectContext`/MoLang engine (the same system driving move animations in battle), usable against arbitrary entities outside of battle. `playTimeline(id, level, users, targets, queries)` runs a datapack-registered `action_effects` timeline (via `ActionEffects.actionEffects`); `playTimeline(timeline, ...)` runs a hand-built/parsed one; `parseTimeline(json)` builds an ad-hoc `ActionEffectTimeline` from a JSON string using Cobblemon's own `ActionEffects.gson` (same keyframe schema: `animation`, `molang`, `sequence`, `parallel`, `entity_particles`, `entity_sound`, `move_to_target`, etc.) without needing a real datapack file; `resolve()` evaluates a one-off MoLang expression string server-side; `runClientMoLang()` sends raw MoLang strings straight to an entity's client-side posable runtime (pure-string packet, touches no MoLang types at all). Internally builds the `MoLangRuntime`/`UsersProvider`/`TargetsProvider` scaffolding that Cobblemon's own battle code (`MoveInstruction`) builds inline — LuxAPI is the first place this exists as a reusable, non-battle-scoped entry point. **Classpath fix (was flagged in Phase 13):** added `compileOnly("com.bedrockk:molang:1.1.20")` to `cobblemon/build.gradle.kts` — since Cobblemon's `mod` artifact is intentionally `isTransitive = false`, `ActionEffectTimeline`/`MoLangRuntime` directly reference `com.bedrockk.molang.*` in their public signatures, which otherwise triggers a "may be forbidden soon" compiler warning (same one seen in Phase 4). `compileOnly` silences it cleanly with zero runtime effect (the real classes are supplied by Cobblemon's own jar at runtime) — verified via a full `./gradlew build`, zero bedrockk warnings emitted. Implemented.
- [x] **Pre-built Animation Triggers:** `BossFxWrapper.GenericAnimation` + `playAnimation()`/`playCry()`/`playEntityEvent()`. Implemented.
- [x] **Particle & Sound Utilities:** `playSnowstormEffect()`, `playMoveParticle()`, `playSound()`, `spawnParticles()` — comprehensive wrapper around Cobblemon's Snowstorm packets and vanilla particles/sounds. Implemented.

## 🟢 Phase 10: Cobblemon Feature Coverage Gaps (Completed)
*Verified against Cobblemon `1.7.3` source — LuxAPI currently has zero wrapper for these, even though the underlying Cobblemon API exists. (Breeding was checked and does not exist in Cobblemon 1.7.3 — not a gap.)*
- [x] **Pasture/Daycare Wrapper:** `PastureManager` (`pasture/PastureManager.kt`) — thin facade over Cobblemon's real `PokemonPastureBlockEntity` (`tether`/`releasePokemon`/`releaseAllPokemon`), the actual authoritative pasture storage. `getTetheredPokemon()` returns a `PastureTethering` DTO snapshot instead of exposing Cobblemon's internal `Tethering` type. **Scope note:** no global pasture registry — Cobblemon doesn't have one either (state lives on the block entity itself), so devs resolve a pasture from a known `BlockPos` via `getPastureAt()`, same as Cobblemon's own block-interaction code. Implemented.
- [x] **Riding API Wrapper:** `RidingInterceptor` (`riding/RidingInterceptor.kt`) — `onRideAttempt()`/`onRideCompleted()`/`onStaminaApply()` wrap Cobblemon's real `CobblemonEvents.RIDE_EVENT_PRE`/`RIDE_EVENT_POST`/`RIDE_EVENT_APPLY_STAMINA`, same cancelable-listener pattern as Phase 7/8's `SpawnInterceptor`/`TradeInterceptor`. `RidingQuery` (`riding/RidingQuery.kt`) separately exposes live ridden-state lookups (`isBeingRidden`, `getRider`, `getRidingStyle`, `getStamina`, `getRideStat`, `dismount`) — read-only, no event angle, so kept out of the interceptor. Implemented.
- [x] **Regional/Multi-Dex Pokédex Support:** `PokedexManager` (`progression/PokedexManager.kt`) rewritten — real `PokedexEntryProgress` enum comparisons replace the old string-matching, and every count/percentage function now accepts an optional `dexId: ResourceLocation?` scoping the query to one of Cobblemon's real regional dexes (`getAvailableDexes()` lists them all: kanto, johto, hoenn, sinnoh, unova, kalos, alola, galar, paldea, hisui, national, unknown). Delegates to Cobblemon's own `CaughtCount`/`SeenCount`/`CaughtPercent` calculators rather than reimplementing the math — no more hardcoded `totalAvailableSpecies = 1025` default. **Breaking change (user-approved):** `getCompletionPercentage`'s old `totalAvailableSpecies: Int` parameter is gone, replaced by `dexId`; no deprecated overload kept, consistent with this project's precedent of prioritizing correctness over silent backward compatibility. Implemented.

## ⚪ Phase 11: Cobblemon Subsystem Coverage (Backlog, Demand-Driven)
*Not a completeness checklist — Cobblemon's `common/.../api/` has 48 subsystem packages and LuxAPI is not obligated to wrap all of them. Skip anything that's Cobblemon's own internal plumbing or duplicates what `commons`/`core` already provide (`codec`, `net`, `orientation`, `reactive`, `scheduling`, `serialization` internals, `conditional`, `asset`, `multiblock` — no dev-facing use case, wrapping them is speculative surface with no add value). Only pull an item from this list into a numbered phase when a plugin dev actually needs safe read/mutate/intercept access to it — same bar Phases 3-10 already used for `pasture`/`riding`.*
- [ ] `fossil` — fossil resurrection wrapper (zero coverage today)
- [ ] `mark` — Pokémon marks API (zero coverage today)
- [ ] `tms` — TM/TR item system wrapper
- [ ] `starter` — starter-Pokémon-selection hook
- [ ] `cooking` / `berry` / `mulch` — curry/farming loop; natural fit to extend the existing `economy` module rather than a standalone one
- [ ] `abilities` — custom ability API (zero coverage; same tier as `moves`/`types` below — core custom-content extension point, not a cosmetic system)
- [ ] `moves` — custom move API (zero coverage)
- [ ] `types` — custom Pokémon type API (zero coverage)
- [ ] `pokeball` — custom pokeball API (zero coverage; distinct from `apricorn` crafting below)
- [ ] `apricorn` — apricorn/pokeball crafting mechanic
- [ ] `drop` — fainting/catch drop-table rules
- [ ] `fishing` — fishing encounter mechanic
- [ ] `permission` — Cobblemon's own permission-node checks; plausible gate for custom commands/actions
- [ ] `tags` — species/type tag queries ("is this species in tag X")
- [ ] `toast` — toast notification system

**Explicitly not gaps (overlap existing LuxAPI coverage, skip re-wrapping):** `snowstorm` (already used via `BossFxWrapper.playSnowstormEffect()`), `properties` (already used via `PokemonSpecParser`), `gui` (LuxAPI has its own `GuiBuilder`/`PaginatedGuiBuilder` — wrapping Cobblemon's own GUI framework too would duplicate, not extend).

## ⚪ Phase 12: Platform & Commons API Parity (Backlog, EnvyWare/API used as reference only — not copied)
*Verified 2026-08-13 against EnvyWare/API's actual module trees (`spigot/.../envyful/api/spigot`, `commons/.../envyful/api`, via GitHub API) and LuxAPI's own `commons`/`core`/`discord`/`fabric`/`neoforge`/`bukkit` source trees. Per `[[project_positioning]]`, EnvyWare is a comparison point only — nothing here is a port, only a gap check.*
- [ ] **`bukkit` platform depth (real gap, not just test debt — supersedes the old "test debt" framing):** EnvyWare's `spigot` platform module covers 8 categories per-platform: `chat`, `config`, `event`, `gui`, `item`, `listener`, `platform`, `player`. LuxAPI's `bukkit` module has exactly one: `command/*`. Missing entirely — not untested, doesn't exist: a GUI bridge (`fabric`/`neoforge` both have `FabricGui`/`NeoForgeGui` + builders + `LuxMenu`/`NeoForgeMenu`; `bukkit` has none), an event bridge (`FabricEventBridge`/`NeoForgeEventBridge` wire native platform events into `commons`' `EventBus`; `bukkit` has none), a player wrapper (`FabricLuxPlayer`/`NeoForgeLuxPlayer` + platform `PlayerManager`; `bukkit` has none), a scheduler bridge (`FabricLuxScheduler`/`NeoForgeLuxScheduler`; `bukkit` has none). `core` (item/particle/sound/bossbar/scoreboard builders) can't fill this gap either — confirmed via `core/build.gradle.kts`: it depends directly on `minecraft("com.mojang:minecraft:1.21.1")` via architectury loom, i.e. built against vanilla Mojang-mapped NMS, which Bukkit/Spigot doesn't share. A Bukkit-native `core` equivalent would need to be built from scratch, not borrowed.
- [ ] **No auto-registration / classpath scanning:** EnvyWare's `commons` has a dedicated `scanning` package. LuxAPI's `AbstractCommandManager`/`CommandManager` requires fully manual `.register(CommandInstance)` calls per class (confirmed via grep — zero scanning/reflection auto-discovery anywhere in `commons/command`), matching LuxCEB's own manual `commandManager.register(LuxCEBCommand(this))` usage. Not blocking, but worth considering for devex parity — annotation-driven auto-registration is a common expectation in this category of API.
- [ ] **No leaderboard system:** EnvyWare's `commons` has a generic `leaderboard` package. LuxAPI has no equivalent — a common ask for game-server dev APIs (top balance/kills/playtime rankings). Worth considering as a `commons` addition.
- [x] ~~Database module structure~~ **Checked 2026-08-13, no gap:** EnvyWare splits DB support into separate `h2`/`sqlite` modules with shared interfaces in `commons`. LuxAPI's single `database` module (`HikariDatabaseProvider` + `DatabaseService` interface + `AttributeManager`/`PersistentAttribute`) is JDBC-driver-agnostic via HikariCP — a simpler, equally valid design, not a gap. Noted as checked, not skipped by oversight.
- [ ] **Minor EnvyWare `commons` categories with no LuxAPI equivalent (low priority):** `json` (shared serialization helpers), `type` (type-safe conversion utilities), `reflection` (reflection helpers), `registry` (generic registry pattern), `concurrency` (async/future utilities). None are blocking and none have a concrete dev ask behind them yet — listed for completeness, same "pull in only on demand" rule as Phase 11's Cobblemon backlog.

## 🟡 Phase 13: Module Fixes & Process Cleanup
*Split 2026-08-13 from the original Phase 13 ("Distribution & Production Readiness") — module-level fixes, versioning drift, and doc/process items that are neither test coverage nor distribution itself. Verified 2026-08-13 against EnvyWare/API's actual README (`maven.envyware.co.uk` remote host, LGPL-3.0, Codacy quality badge, Discord `#api-changelog`) and Cobblemon's own GitLab repo (`.gitlab-ci.yml` build gate, root `CHANGELOG.md` + `changelogs/` dir).*
- [x] ~~LuxAPI/LuxCEB Version Drift — `economy` and `bukkit`~~ **Checked 2026-08-13, no-op for these two:** `economy` has exactly one commit ever (`b936293`) — never touched since creation, so `1.2.1`→`1.2.4` is just the shared root version stamp, not code drift; `LuxCEB/VaultProvider.kt` still matches `LuxEconomyCore`'s current interface exactly. `bukkit`'s own last commit is literally titled "Update version to 1.2.1" with nothing after it — same situation. The GTS dupe/PC-desync fixes referenced in the original concern both live in `cobblemon`, which LuxCEB doesn't depend on — never a real risk.
- [ ] **LuxAPI/LuxCEB Version Drift — `commons` (still open, real surface):** Unlike `economy`/`bukkit` above, `commons` has 55 commits of real churn, and LuxCEB's `LuxCEBCommand.kt` imports it directly (`@Command`, `@SubCommand`, `@TabComplete`, `CommandSender`, `TabHandler`, `TabUtils`) — not just transitively through `bukkit`. Whether commons' command-annotation API shape changed between LuxCEB's pinned `1.2.1` and current `1.2.4` is unverified. Check before assuming LuxCEB still compiles against a fresh `commons` publish.
- [ ] **Two minor findings from the 2026-08-13 code-level audit:** `core/loot/LootManager.kt:24-27` — `distribute(lootId, ...)` silently no-ops if `lootId` isn't registered, no log/exception, easy to debug-blind in production. `commons/config/LuxConfig.kt:40-52` — `reload()` reflectively copies every declared field except the literal `"configFile"` from a freshly-loaded instance onto `this`; any future non-persisted/computed field on a `LuxConfig` subclass would get silently clobbered on reload. No such field exists today (currently inert), but worth a guard before one gets added.
- [ ] **Discord Module — Re-scope or Remove:** `discord/src` currently has zero source files (only a stale `.iml` and leftover local `build/` artifacts up to v1.2.4, suggesting it had real content before). `commons` already ships `DiscordWebHook`/`DiscordEmbed` (Phase 2). Before rebuilding this module, decide what it should contain that isn't already covered by `commons` — otherwise remove the empty stub from `settings.gradle.kts` rather than leaving a dead module.
- [ ] **Version/Changelog Sync:** `README.md` states `Version: 1.1.1`, but root `build.gradle.kts` is actually at `1.2.4` — docs are stale relative to the build. Establish a single source of truth. **Recommendation:** don't copy EnvyWare's Discord-only `#api-changelog` — Cobblemon's own model (a root `CHANGELOG.md` + `changelogs/` dir, versioned in-repo and discoverable without Discord access) is the better bar to match.
- [ ] **Quality Badge:** EnvyWare's README carries a Codacy grade badge as a visible trust signal. Add an equivalent static-analysis badge (Codacy, CodeClimate, or similar) once the CI gate (Phase 14) exists to feed it.
- [ ] **`economy` Module — Correct Its Role:** `economy` (`LuxEconomy`/`LuxEconomyManager`, package `com.novaco.economy`) is not a stray standalone product — it's the Bukkit-side core-dependency plugin, the LuxAPI equivalent of **EverNifeCore** in the Pixelmon ecosystem. It has no `commons`/`core`/Cobblemon coupling by design. README.md:28's "optional standalone economy plugin layer" description undersells this — update it to name the intended architecture. **Corrected 2026-08-13, mechanism confirmed by reading LuxCEB's source (`...\Project\LuxCEB`, see `[[luxceb-companion-repo]]` memory):** the bridge plugin isn't missing — it's `LuxCEB`. Its Cobblemon-side coupling is **not** through LuxAPI's Fabric/NeoForge `cobblemon` module (`com.novaco.luxapi:cobblemon` is not a LuxCEB dependency, confirmed) — `CobbleDollarsBridge.kt` reflects a Bukkit `Player`'s NMS handle and casts it directly to `fr.harmex.cobbledollars.common.utils.CobbleDollarsPlayer`, calling its mixin-injected `` `cobbleDollars$getCobbleDollars`() ``/`` `cobbleDollars$setCobbleDollars`() `` accessors — a hybrid-server (Mohist/Arclight-style) reflection trick against the third-party CobbleDollars mod, entirely bypassing LuxAPI's `cobblemon` module. `GtsBridge`/`HuntBridge` do the same against two more third-party Bukkit plugins (`org.pokesplash.gts`, `org.pokesplash.hunt`). LuxAPI's only actual role in LuxCEB is `economy`'s `LuxEconomyManager`/`VaultProvider` (Vault-as-backend plumbing) and `bukkit`'s `BukkitCommandManager` (registers `/luxceb`) — `cobblemon` module involvement: zero, confirmed not inferred.
- [ ] **Shadow Jar Artifact Name (needs clarification, not assumed a bug):** `bukkit/build.gradle.kts` and `economy/build.gradle.kts` both set `shadowJar.archiveBaseName = "LuxEcoCore"`, so each currently produces its own separate `LuxEcoCore-<version>.jar` in its own module's `build/libs/`. Given `economy`'s actual role above, clarify whether `bukkit` and `economy` are meant to ship as one combined `LuxEcoCore` artifact (in which case the build should merge them into a single publish target) or as two independently-versioned jars that happen to share a name (in which case rename one).
- [ ] **README.md Phase-Status Desync:** `README.md:165` says "Phase 8 (Economy & Trade) has a known critical gap flagged above" and "Phases 10-11 ... are open" — both stale relative to this file: Phase 8's GTS dupe bug is fixed and Phase 10 is complete. The two docs currently contradict each other; sync README's status line to this file whenever a phase flips state.

## 🟡 Phase 14: Test Coverage
*Split 2026-08-13 from the original Phase 13 — CI test gate and outstanding test-coverage gaps, isolated ahead of the distribution phase so publishing never ships on unverified code.*
- [ ] **CI Build/Test Gate (highest priority):** The only existing workflow (`.github/workflows/discord_notify.yml`) posts a Discord changelog embed on push — it does not run `./gradlew build`/`test`. Add an actual CI workflow that compiles and runs tests on every push/PR, so regressions like the `isPerfect()` bug and the GTS stub don't reach `main` unnoticed. Cobblemon itself gates on `.gitlab-ci.yml` — same bar.
- [x] ~~Real Test Coverage~~ **Stale as of 2026-08-13 — corrected below.** This item previously claimed "the entire `cobblemon` module has exactly one test file (`EvolutionHookManagerTest.kt`), and it's `@Disabled`." That's no longer true: `cobblemon/src/test/kotlin` now has 12 test files (`LuxCobblemonTest`, `TradeTaxManagerTest`, `EvolutionHookManagerTest`, `GlobalTradeManagerTest`, `TradeListingTest`, `PokemonPropertyManagerTest`, `CatchStreakManagerTest`, `PokedexManagerTest`, `AreaSpawnManagerTest`, `LuxAreaSpawningInfluenceTest`, `SwarmEventTest`, `PCStorageManagerTest`), and `commons`/`core`/`database`/`fabric`/`neoforge` are all well-covered too. Do not re-cite the old "0% coverage" claim.
- [ ] **Remaining Test Coverage Gaps:** Of `cobblemon`'s 95 source files, the interceptor/event-hook pattern — the trickiest thing in this codebase to get right (cancelable listeners, poll-task cleanup, dupe-safety) — is still untested where it matters most: `BattleInterceptor`, `BattleBuilder`, `BattleRuleManager`, `SpawnInterceptor`, `TradeInterceptor`, `RidingInterceptor`, `PastureManager`, `HookManager`. Prioritize these over broad-but-shallow coverage elsewhere. **Also found 2026-08-13:** `fabric`/`neoforge` both lack a `*GuiBuilderTest`/`*PaginatedGuiBuilderTest`/`*LuxTaskTest` for their `FabricGuiBuilder`/`NeoForgeGuiBuilder` classes (symmetric gap between the two, not an asymmetry). More importantly: no test anywhere exercises a *denied* permission path against a real `FabricLuxPlayer`/`NeoForgeLuxPlayer` — impossible today since the stub can't return `false` (see the CRITICAL section at the top of this file); add one as part of that fix, not after.
- [ ] **`economy` Module Test Debt:** Verified 2026-08-13 — `economy` sits at 0% test coverage (3 source files/0 tests), untracked until now. (`bukkit`'s coverage gap is superseded by Phase 12's platform-depth item — it's not just missing tests, most of the platform layer itself doesn't exist yet.)

## 🟡 Phase 15: Distribution
*Split 2026-08-13 from the original Phase 13 — the real publishing gap, isolated so this final phase contains distribution work only.*
- [ ] **Remote Maven Publishing (confirmed blocking, not hypothetical):** The `maven-publish` plugin is already wired up repo-wide (see root `build.gradle.kts:70-75`) and produces a working `MavenPublication` per module — but `repositories { maven { name = "LocalMaven"; url = .../build/repo } }` only publishes to a local build directory, never to a real remote host. **Verified 2026-08-13:** this is not just a maturity gap — LuxCEB (`...\Project\LuxCEB`, separate repo, see `[[luxceb-companion-repo]]` memory) already depends on `com.novaco.luxapi:{economy,commons,bukkit}:1.2.1` via `repositories { mavenLocal() }`, meaning it currently only builds if `./gradlew publishToMavenLocal` has been run in LuxAPI first (Gradle's `maven-publish` plugin auto-registers that task regardless of the declared `repositories{}` block — it's separate from the custom `LocalMaven`/`build/repo` target above). That workflow is undocumented anywhere. Add a real remote repository target (e.g., a self-hosted Reposilite/Nexus instance or GitHub Packages) with credentials, matching EnvyWare's `maven.envyware.co.uk` model — this replaces the fragile "remember to `publishToMavenLocal`" step for both LuxCEB and any future consumer.

---
*Roadmap is subject to change based on Cobblemon's upstream API updates.*
